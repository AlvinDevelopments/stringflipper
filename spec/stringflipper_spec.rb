require 'spec_helper'

describe Stringflipper do
  it 'has a version number' do
    expect(Stringflipper::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
